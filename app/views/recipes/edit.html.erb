<% if @recipe.errors.any? %>
  <div class="container">
    <%= render 'errors' %>
  </div>
<% end %>

<div class="container">
  <p style="color:red">Edit your comment</p>
  <%= bootstrap_form_for(@recipe) do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>
