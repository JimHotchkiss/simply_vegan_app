<div class="container">
  <%= render 'comments/errors' %>
</div>

<div class="container">
  <p style="color:red">Contribute a Comment to DYI</p>
  <%= bootstrap_form_for(@comment) do |f| %>
      <%= render 'form', f: f %>
  <% end %>
</div>
