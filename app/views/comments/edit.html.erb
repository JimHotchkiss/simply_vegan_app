<div class="container">
  <%= render 'comments/errors' %>
</div>

<div class="container">
  <p style="color:red">Contribute a Comment to Simply Vegan</p>
  <%= bootstrap_form_for(@comment) do |f| %>
      <%= render 'form', f: f %>
      <%= f.submit 'edit comment' %>
  <% end %>
</div>
